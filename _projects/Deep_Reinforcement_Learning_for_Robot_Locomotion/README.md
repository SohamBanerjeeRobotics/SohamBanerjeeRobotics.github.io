# Image notes

`network_architecture_code.jpg` was originally a 270×150px screenshot —
too low-res to read comfortably. Rebuilt from scratch as a matplotlib
diagram reproducing the same Actor/Critic layer structure and labels at
2800×1520, so it's sharp at any display size.

`addpg_pseudocode.jpg` is the original screenshot, upscaled 2x (Lanczos)
with a mild unsharp mask to counter the JPEG softness — same content,
just crisper.
